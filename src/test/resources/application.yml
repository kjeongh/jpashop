spring: #설정이 없으면 기본적으로 스프링부트가 메모리모드로 돌린다
#  datasource:
#    url: jdbc:h2:mem:test #인메모리 모드로 동작. 실제 운영에 쓰는 db와 분리
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop #디폴트로 create-drop함. create는 먼저 다 drop하고 create하고 실행, create-drop은 애플리케이션 종료시점에 drop.
#    properties:
#      hibernate:
#        #show_sql: true
#        format_sql: true


logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
